// fetch is available globally in Node 18+

async function test() {
  try {
    // 1. Get a token or mock auth. 
    // Wait, the API requires authentication. 
    // Is there a test file I can use?
  } catch (err) {
    console.error(err);
  }
}
test();
